<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head.ejs') %>

  <body>
    <%- include('partials/header.ejs', { user: user }) %>
    <main class="container is-fullhd mt-5 mt-9">
      <div class="columns">
        <% for(let i = 0; i < doces.length; i++) { %> <%-
        include('partials/card.ejs', { value: doces[i] }) %> <% } %>
      </div>
    </main>
  </body>
</html>
